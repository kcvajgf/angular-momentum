.row-fluid(ng-show="problem.has_answered")
  div(ng-show="problem.answer != null") Answer: {{problem.answer}}
  div(ng-show="problem.answer == null")
    i.icon-spinner.icon-spin
    |  Loading Answer...
.row-fluid(ng-show="!problem.has_answered && problem.can_answer")
  div Answer:
  form.input-append(ng-submit="submit()")
    input(ng-model="answer", placeholder="Answer here...")
    button.btn.btn-primary(ng-disabled="!answer.length", type='submit') Submit
