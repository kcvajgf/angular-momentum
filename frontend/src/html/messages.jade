div
  a(href="/#/message") Click here to edit messages
  .row
    h3 Messages
    #messages
      .row(ng-repeat="message in messages")
        .span1.id
          strong ID
          p {{message.id}}
        .span3.message
          strong Message
          p {{message.message}}
